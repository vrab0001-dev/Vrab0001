name: System Sync

on:
  schedule:
    - cron: '0 0 * * *'       # Every night at midnight UTC
  workflow_dispatch:            # Manual trigger from GitHub UI
  repository_dispatch:          # Triggered by raid repo push
    types: [raid-complete]

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Profile Repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: pip install requests

      - name: Run System Engine
        env:
          GITHUB_TOKEN: ${{ secrets.RAID_TOKEN }}
        run: python system_engine.py

      - name: Commit Changes
        run: |
          git config --local user.email "system@vrab0001.dev"
          git config --local user.name "The System"
          git add README.md system_state.json
          git diff --quiet && git diff --staged --quiet || \
            (git commit -m "⚡ System: Stats Updated" && git push)